kind: Deployment
apiVersion: apps/v1
metadata:
  name: risk-engine-java
spec:
  replicas: 1
  selector:
    matchLabels:
      app: risk-engine-java-app
  template:
    metadata:
      labels:
        app: risk-engine-java-app
    spec:
      containers:
        - name: risk-engine-java
          image: risk-engine-java-image:latest
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
          resources:
            requests:
              memory: "300Mi"
              cpu: "50m"
---
kind: Service
apiVersion: v1
metadata:
  name: risk-engine-java-svc
spec:
  ports:
    - name: http-8081
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: risk-engine-java-app